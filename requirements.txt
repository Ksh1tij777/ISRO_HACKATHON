# Route Resilience — dependencies
# NOTE: torch/torchvision are assumed already installed with CUDA in the active
# environment (torch 2.11.0+cu128, RTX 4060). Do NOT `pip install torch` here or
# you may clobber the CUDA build with a CPU wheel. Install the rest with:
#   pip install -r requirements.txt

# --- CV half (AA) ---
segmentation-models-pytorch>=0.3.3
transformers>=4.40
albumentations>=1.4
opencv-python-headless>=4.9
rasterio>=1.3
scikit-image>=0.22
numpy>=1.26
pyyaml>=6.0
tqdm>=4.66

# --- Graph half (Amrit) ---
networkx>=3.2
osmnx>=1.9
geopandas>=0.14
shapely>=2.0
scipy>=1.12
matplotlib>=3.8
streamlit>=1.33
folium>=0.16
streamlit-folium>=0.20

# --- dev / test ---
pytest>=8.0
