# Data & model artifacts (never commit — large binaries)
# Anchored to repo root so src/data/ (source code) is NOT ignored.
/data/
/runs/
*.tif
*.tiff
*.pth
*.pt
*.graphml
*.pkl
*.pickle

# Python
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# osmnx / tile download cache
cache/

# Office temp lock files + superseded deck drafts (keep only the Final deck)
~$*
/Route_Resilience_BAH2026_Idea.pptx
/Route_Resilience_BAH2026_Idea_CLEAN.pptx
/Route_Resilience_BAH2026_Pitch.pptx
